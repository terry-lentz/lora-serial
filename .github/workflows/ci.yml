name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Cache PlatformIO
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio
            ~/.cache/pip
          key: pio-${{ runner.os }}-${{ hashFiles('platformio.ini') }}

      - name: Install PlatformIO
        run: pip install --upgrade platformio

      - name: Native unit tests (link layer + modem sim)
        run: pio test -e native

      - name: Build firmware image (XIAO)
        run: pio run -e node_raw

      - name: Build Wio Tracker L1 (nRF52) + UF2
        run: |
          pio run -e wio_l1
          tools/make_uf2.sh wio_l1
